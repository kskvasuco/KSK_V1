# ─── API Server URL ────────────────────────────────────────────────────────────
# PRODUCTION (Render-hosted backend — used in APK/EAS builds via eas.json env)
EXPO_PUBLIC_API_URL=https://ksk-v1.onrender.com

# DEVELOPMENT (your PC's LAN IP — only needed when running with Expo Go on local network)
# EXPO_PUBLIC_API_URL=http://192.168.1.100:5500
